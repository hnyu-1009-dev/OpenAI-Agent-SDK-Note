"""
这一组示例讲 MCP（Model Context Protocol）。

你可以把 MCP 理解为：
“一种让模型 / Agent 以标准方式访问外部工具和数据源的协议层”。

为什么 MCP 很重要？

因为在真实项目里，工具并不总是和 Agent 写在同一个 Python 文件里。
很多时候工具能力会来自：

- 本地独立进程
- 局域网服务
- 远程 HTTP 服务
- 第三方平台

MCP 的价值就在于把这些能力统一抽象成标准可连接的服务。
"""
