"""
这一组示例讲多 Agent 协作。

主要包含两种经典模式：

1. Agent as Tool
   把某个专家 Agent 当作“工具”供上层 Agent 调用。
   这种模式下，总控 Agent 仍然掌握最终回复权。

2. Handoff
   把控制权真正交给另一个 Agent。
   一旦 handoff 发生，后续主导回答的就不再是原来的 Agent。

这两种模式看起来相似，但设计哲学完全不同：

- `as_tool` 更像“经理调用专家拿结果”
- `handoff` 更像“前台把客户转接给真正负责的专家”
"""
