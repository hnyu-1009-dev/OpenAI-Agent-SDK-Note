"""
这一组示例讲 Agent 的运行方式。

同一个 Agent，通常可以用三种方式执行：

1. `Runner.run_sync`
   同步方式，最容易上手，适合脚本和简单演示。
2. `Runner.run`
   异步方式，适合现代 Python 服务和更复杂的并发场景。
3. `Runner.run_streamed`
   流式方式，适合需要边生成边显示输出的交互界面。

理解这三者的差别，比记住某个 API 名更重要。
"""
