"""
`03_agents` 这一章进入 OpenAI Agents SDK 的核心主题：Agent。

你可以把 Agent 理解为“带有角色设定、能力边界、可调用工具和运行状态的智能体对象”。
相比前面直接调用模型接口，这一层抽象更适合搭建真正可扩展的 AI 应用。

这一章目前主要覆盖：

1. 单 Agent 的模型接入
   学习如何把 Agent 绑定到某个模型，或者绑定到某个模型提供器。
2. Agent 的运行方式
   学习同步执行、异步执行、流式执行的差异。
3. Agent 的工具集成
   学习如何用 `@function_tool` 暴露本地函数给 Agent 使用。
4. Agent 的会话管理
   学习如何让 Agent 记住上下文，甚至把上下文持久化到 SQLite 数据库。
"""
